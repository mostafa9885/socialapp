

import 'package:flutter/material.dart';

class FeelPostScreen extends StatelessWidget {
  const FeelPostScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
