

import 'package:flutter/material.dart';

class TagPostScreen extends StatelessWidget {
  const TagPostScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
